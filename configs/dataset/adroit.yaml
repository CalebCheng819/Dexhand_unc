defaults:
  - adroit_pen.yaml
  - _self_

num_demos: 15
action_type: 'rot_mat'  # ['joint_value', 'rot_quat', 'rot_6d', 'rot_vec', 'rot_euler','rot_mat']
obs_horizon: ${model.obs_horizon}
pred_horizon: ${model.pred_horizon}

batch_size: 128
num_workers: 0
drop_last: True

eval_episodes: 50     #增加eval数量